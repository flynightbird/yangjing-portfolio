# About AI Continuous Signal Design

## Goal

Replace the generic prompt-to-dashboard illustration in the AI capability card with an authored continuous workflow visual.

## Locked Composition

- Use direction A, a continuous signal path connecting product judgment, prototype, test, and refinement.
- Remove the prompt input box, sparkle, one-way arrow, loading dots, and fake dashboard bars.
- Use the refined Visual System module language from direction C at both endpoints.
- The start module is an open design structure; the end module is a resolved interface system.
- Keep the existing dark card, green accent, copy, rounded frame, and responsive card layout.
- Animate the signal moving through the path and use restrained endpoint activity.
- Disable new animation under `prefers-reduced-motion`.

## Verification

- Component tests confirm the AI card contains the continuous signal and both endpoint systems.
- Obsolete sparkle and transform arrow contracts are removed.
- Desktop and mobile screenshots confirm a balanced graphic with no clipping or horizontal overflow.
